function set_current_page( changeto ){

    // console.log(" current receiver action is running is running" , payload);
       
        return {
            type : changeto 
            
           
        }
    
    }

    export default set_current_page